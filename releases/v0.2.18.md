# v0.2.18

## Fixes
- Release-infra fix only: corrected the new Save As E2E test to derive its Windows-detection expectation from the browser's user agent (matching how the app decides to show the Open in Notepad++ button) instead of the operating system running the test. No application code changed from v0.2.17; v0.2.17 never published because its CI run caught this test-only issue.

## Platform Notes
- Windows in-app updates continue to use the packaged updater contract (installer + blockmap + `latest.yml`); macOS and Linux releases remain manual downloads.

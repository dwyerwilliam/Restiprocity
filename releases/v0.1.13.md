# v0.1.13

## Bug Fixes

### NTLM Request Diagnostics
- Preserve meaningful native Electron network failures when they race the request timeout.
- Keep timeout classification for abort-induced errors, so cancelled transport events do not hide the configured timeout.
- Add request-engine lifecycle coverage for native error, timeout, and abort-error settlement paths.

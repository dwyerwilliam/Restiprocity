# v0.2.13

## Fixes
- XML, text, and HTML raw body editors now fill the entire remaining vertical space of the middle frame, matching the JSON editor's full-height layout instead of stopping at a fixed minimum height.

## Platform Notes
- Windows in-app updates continue to use the packaged updater contract (installer + blockmap + `latest.yml`); macOS and Linux releases remain manual downloads.
